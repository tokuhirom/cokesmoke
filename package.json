{
  "name": "coalsmoke",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "oxlint -c .oxlintrc.json src/",
    "fmt": "oxfmt --write src/",
    "fmt:check": "oxfmt --check src/",
    "prepare": "husky"
  },
  "dependencies": {
    "rot-js": "^2.2.0"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "oxfmt": "^0.40.0",
    "oxlint": "^1.55.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
